# Behavioral / Leadership — Prepared Answers

STAR-structured answers keyed to [`question-bank.md`](question-bank.md). Each answer **cites
the `profile/stories/` file** (or role/project) it draws from, so it stays truthful and
reusable — never invent facts to make an answer land (Golden Rule: never fabricate).

Fill these during mock interviews (the `interview-prep` skill): the agent asks the question, the user
answers from real experience, the agent structures and refines it here, and creates any
missing story in `profile/stories/` (the story-elicitation loop).

> Status per answer: `drafted` → `refined` → `rehearsed`.
>
> **Privacy:** alias every third party by role (`my report`, `a peer lead`, `the skip-level`);
> never a real name. Keep employer specifics generic or ratioed (see `CLAUDE.md` privacy rule).

---

## Q{N} — {question theme}
- **Source story:** TODO(link `profile/stories/*.md`)
- **Status:** _(empty)_

**S:** TODO — situation (real context; alias any names)
**T:** TODO — task / what was yours to do
**A:** TODO — action you took (specific, concrete)
**R:** TODO — result, with a **number** where possible; `TODO(user)` if the metric is missing

---

<!--
Copy the block above per question as it gets prepared. No need to pre-create all of them —
add a section when a question is worked. Keep the `Q#` ids aligned with `question-bank.md`.

Framing reminders:
- "Raised the quality bar" answers: for the TEAM/ORG, not "I have high standards"; end on a
  measured outcome.
- People/scenario answers ("how would you handle X"): a concrete, real, numbers-backed STAR —
  not a philosophy. Generalities are a common reject reason.
- "What would you add / how are you different": give the DELTA, not similarity/fit.
-->
